# Dados de conex�o com o banco mysql
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/benft_db
    username: root
    password: 123456
  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true
  flyway:
    baseline-on-migrate: true